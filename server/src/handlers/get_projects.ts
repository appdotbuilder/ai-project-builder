import { type Project } from '../schema';

export async function getProjects(userId: number): Promise<Project[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is to:
    // 1. Query all projects belonging to the specified user
    // 2. Return array of projects ordered by updated_at desc
    // 3. Ensure user can only see their own projects
    
    return Promise.resolve([]);
}